**Requirements:**

- Create a grid of images where each row contains four images on screens wider than 1024 pixels.
- On screens between 768 pixels and 1024 pixels wide, display three images per row.
- On screens narrower than 768 pixels, display two images per row.
- Use media queries to implement this responsive image grid.
